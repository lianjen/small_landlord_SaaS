# Design System - Rental SaaS 2026

## 核心體驗目標
我們追求的是一種「無壓力」的管理體驗。房東打開系統時，應該感到一切盡在掌握，而不是被密密麻麻的表格嚇到。

## 視覺規範 (Visual Standards)

### 1. 色彩配置 (Color Palette)
- **Primary**: `#4F46E5` (Indigo/Trust) - 用於主要按鈕與標題。
- **Background**: `#F9FAFB` (Off-white) - 減輕視覺疲勞。
- **Surface**: `#FFFFFF` (Pure White) - 用於卡片背景。
- **Success**: `#10B981` (Emerald) - 已繳租/正常狀態。
- **Warning**: `#F59E0B` (Amber) - 即將到期。
- **Danger**: `#EF4444` (Red) - 逾期/刪除動作。

### 2. 佈面配置 (Layout)
- 頁面頂部保持適當留白 (1rem)。
- 關鍵指標 (KPI) 使用卡片式設計。
- 列表與表格應具備視覺階層，次要資訊使用灰色細體。

## 元件樣式 (Components)

### 1. 狀態標籤 (Status Badges)
- 🟢 **已繳租**: 代表安全、完成。
- 🔴 **逾期**: 代表警示、需要行動。
- 🟡 **即將到期**: 代表提醒。

### 2. 卡片視圖 (Tenant Cards)
- 顯示房客姓名 (大字體)
- 房號 (右上角標籤)
- 租約到期日 (灰色小字)
- 快速操作按鈕 (繳費、詳細)

### 3. CSS 注入指令
```css
/* 移除頂部留白 */
.block-container {
    padding-top: 2rem;
}

/* 指標卡片樣式 */
[data-testid="stMetric"] {
    background-color: #ffffff;
    padding: 1rem;
    border-radius: 0.75rem;
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
    border: 1px solid #e5e7eb;
}
```
